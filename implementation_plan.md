# 🚀 기능 개선 구현 계획서

요청하신 세 가지 핵심 기능(문제 즉시 분할, 표지 수정, PDF 업로드)에 대한 구현 계획입니다.

## 📝 User Review Required
이 작업은 프론트엔드와 백엔드 양쪽을 수정해야 합니다. 아래 계획을 확인하시고 승인(Proceed)해 주시면 즉시 개발을 진행하겠습니다.

---

## 🛠️ Proposed Changes

### 1. `templates/ebsi_sc.html` (교재 화면에서 즉시 이미지 분할)
교재의 문제를 하나씩 확인하는 화면에서 바로 문제를 자를 수 있도록 버튼을 추가합니다.
- **[MODIFY]** `renderGrid()` 함수 내의 각 문제 카드 하단(또는 우측 상단)에 `✂️ 자르기` 버튼 추가.
- **[NEW]** `splitProblem(index)` 자바스크립트 함수 추가:
  - 실행 시 `confirm()` 창을 띄워 "진짜로 자르시겠습니까? 한번 자르면 되돌릴 수 없습니다." 경고 메시지 출력.
  - 확인을 누르면 백엔드 API (`/api/run/split_sync` 또는 기존 분할 로직) 호출.
  - 분할이 끝나면 `sessionStorage`에 현재의 스크롤 위치(`window.scrollY`)를 저장.
  - 분할 직후 자동으로 에셋 재생성(`book_assets.py`)을 트리거한 뒤, 페이지를 새로고침(`location.reload()`).
  - 새로고침 완료 시 `sessionStorage`에 저장된 위치로 스크롤을 이동하여 자른 문제를 바로 확인할 수 있게 고정.

### 2. `templates/admin.html` & `app.py` (PDF 직접 업로드 기능)
현재 텍스트로 경로를 입력받던 방식을 브라우저의 파일 업로드 기능으로 대체합니다.
- **[MODIFY] `admin.html`**: `<input type="file" id="pdfFile" accept=".pdf">` 버튼 추가.
- **[NEW] `app.py`**: `/api/upload_pdf` 엔드포인트를 신설하여 업로드된 PDF를 서버의 `uploads/` 폴더에 임시 저장.
- 작업 흐름: 파일 선택 -> [추출 시작] 클릭 -> 서버로 PDF 업로드 -> 서버에 저장된 경로를 이용해 기존 SSE(진행률 바) 추출 작업 시작.

### 3. 표지 이미지 커스텀 URL 적용 (관리자 페이지 & `book_assets.py`)
기본적으로 교재의 첫 번째 이미지(`001.png`)가 표지로 사용되는 것을, 사용자가 원하는 링크(URL)로 바꿀 수 있도록 합니다.
- **[MODIFY] `admin.html`**: '교재 에셋 생성' 탭에 `표지 이미지 URL (선택사항)` 입력 폼 추가.
- **[MODIFY] `app.py` & `book_assets.py`**: 에셋을 생성할 때, 폼으로 전달된 URL이 있다면 그 링크를 우선적으로 `index.html` 표지(썸네일)로 덮어쓰도록 로직 수정. (해당 정보는 교재 폴더 내 `meta.json` 같은 파일에 저장하여 지속성을 유지)

---

## 🧪 Verification Plan

1. **이미지 분할 테스트**: 교재 뷰어에서 ✂️ 자르기 버튼을 클릭 -> 경고창 확인 -> 승인 후 로딩 대기 -> 페이지가 새로고침되며 원래 보던 위치로 스크롤이 유지되는지, 문제가 2개로 나뉘었는지 확인.
2. **PDF 업로드 테스트**: 로컬 PDF 파일을 업로드 버튼으로 첨부 후 추출 실행. 파일이 정상적으로 서버에 복사되고 추출이 진행되는지 팝업 게이지로 확인.
3. **표지 변경 테스트**: 에셋 생성 시 웹상의 이미지 링크(예: 구글 이미지 링크)를 넣고 생성한 뒤, 메인 화면(`index.html`)에서 표지가 해당 사진으로 잘 변경되었는지 확인.
